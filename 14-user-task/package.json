{
  "name": "jstest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "roll": "rollup index.js --file bundle.js --format iife",
    "life": "live-server",
    "build": "npm run roll && npm run life"
  },
  "private": true,
  "devDependencies": {
    "live-server": "^1.2.2",
    "rollup": "^4.52.3"
  }
}
